kind: secret
name: {{ include "signals.credentials.name" . }}
description: Elevarq Signals credentials (PostgreSQL password + API bearer token)
tags:
  {{- include "signals.tags" . | nindent 2 }}
type: dictionary
data:
  pg-password: {{ .Values.postgres.password | quote }}
  api-token: {{ .Values.api.token | quote }}
